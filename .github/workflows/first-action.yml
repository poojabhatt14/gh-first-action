name: First Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print Greeting
        runs: echo "Hello world"
      - name: print goodbye
        runs: echo "done -bye"
